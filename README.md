# Rule Composer

A native macOS app for analyzing AI agent system prompts. Parses prompts into behavioral rules, generates test scenarios, and evaluates them against Claude.

Built with Tauri 2, React, TypeScript, and Monaco Editor.

## Development

```bash
npm install
npx tauri dev
```

## Build

```bash
npx tauri build
```
